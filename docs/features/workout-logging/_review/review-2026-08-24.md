# Review record — workout-logging — 2026-08-24

## Scope

`git diff main..HEAD` (base `1f46d76`, HEAD `1e9cf00`). 147 files changed, +6242/-909. Full feature
history (survey → specify → clarify → design → data-model → tasks → implement) lives on this one
branch; `main` has not diverged.

Two independent clean-context reviewers (`sdd:reviewer`, opus, effort high) ran in parallel:
stage 1 (spec §4 user-story + §5 AC end-to-end trace) and stage 2 (conventions, error/edge
handling, security, boundary violations, test adequacy). `npm run typecheck` clean, `npm test`
106/106 pass, `npm run lint` 0 errors / 16 warnings.

## Findings

Legend: **Fix now** = follow-up task for `implement`. **Defer** = recorded in spec §8 Open
questions. **Not an issue** = dismissed with reason, no spec/code change.

| # | Stage | Headline | Location | Verdict |
|---|---|---|---|---|
| 1 | 1+2 | `/log_workout` while a session is open never reaches `WorkoutLoggingRoute`'s "already open" reply — `continueConversation` intercepts it first, the literal command text goes to OpenAI as an exercise description, burning an AC-07 retry. AC-12 unreachable in production. | `routes/routesProcessor.ts:28-35`, `workoutLogging/workoutLoggingConversation.ts:62-90` | **Defer** — record in spec §8, owner+due |
| 2 | 1 | No DB row exists anywhere for prompt `workout_exercise_parser` — every exercise message throws `BadRequestError` against a live DB. | `workoutLogging/workoutExerciseParser.ts:31-38`, `telegram/repository/dictPromptRepository.ts:12-14` | **Not an issue** — `dict_prompt` rows are seeded operationally (manual/admin insert), not via code migration; outside this review's code scope, but the row must exist before this feature is used in production |
| 3 | 2 | `/cancel` and the FAILED-step path deactivate the conversation without firing `onPreempt`/`onExpire` — the durable `workout_log_session` row stays open forever; `startSession` then always returns already-open, permanently locking the client out. | `routes/routesProcessor.ts:118-126`, `conversations/engine.ts:94-102,148-163`, `workoutLoggingService.ts:106-109` | **Fix now** |
| 4 | 2 | Lifecycle hooks aren't error-isolated — a malformed conversation-state row throws inside `preemptActiveConversation`, breaking unrelated routes (`/progress`, `/measurements`, cron reminders). | `conversations/engine.ts:140-146`, `workoutLoggingConversation.ts:268-275` | **Fix now** |
| 5 | 2 | Confirmation callback trusts the parsed id with no membership check against `pending.candidates`; `Number('')` passes the NaN guard (inserts `dict_exercise_id=0`, FK violation); stale buttons stay live since the keyboard is never removed. | `workoutLoggingConversation.ts:134-162` | **Fix now** |
| 6 | 1 | AC-05/US-03 candidate picture is computed (`imageUrl` presigned) but never delivered — `ConversationResponse` has no media field, nothing calls `sendPhoto`. Dead code, untested. | `workoutLoggingConversation.ts:219-254`, `workoutCandidateMatcher.ts:44-63`, `conversations/model.ts:12-16` | **Fix now** |
| 7 | 1+2 | AC-09: `/end_workout` while a confirmation is pending returns "unsupported input" — `WAITING_CONFIRMATION` declares only `onCallback`. Client is stuck (or must use `/cancel`, which orphans the session per #3). | `workoutLoggingConversation.ts:44-51`, `conversations/engine.ts:54-56` | **Fix now** |
| 8 | 1+2 | T5 (ADR-0002) hardcoded `exerciseRepository.search()` offset/limit to `(0,3)`, dropping the caller's `page`/`limit` — the coach exercise-search HTTP API still validates/echoes them but now only ever returns 3 rows with a wrong `total`. Test bakes the regression in. | `coach/exercise/repository/exerciseRepository.ts:11-21`, `coach/exercise/handlers/exercise.ts:17-42`, `workoutCandidateMatcher.ts:31` | **Fix now** |
| 9 | 1 | AC-07: parser collapses missing-numbers / missing-weight / multiple-exercises into one generic "unclear" reply with no reason; multi-exercise case never surfaced. | `workoutLogging/workoutExerciseParser.ts:52-54`, `i18n/locales/en.json` | **Fix now** |
| 10 | 1 | AC-08: on second failed parse the code persists the retry text, not the client's original wording as the AC requires; test encodes the wrong behavior. | `workoutLoggingConversation.ts:97-112` | **Fix now** |
| 11 | 2 | Parser only type-checks reps/sets/weight, not range — `weight:99999` overflows `numeric(5,1)`, zero/negative/huge reps or sets pass through, causing a raw Postgres error instead of the AC-07 "unclear" path. | `workoutExerciseParser.ts:44-57`, migration `02_create_workout_log_entry.up.sql:6-8` | **Fix now** |
| 12 | 2 | No guard against processing the same pending confirmation twice concurrently — entry is saved before the state transition, `updateConversation` filters only on `id`+`is_active`; two callbacks can both `addEntry`. | `workoutLoggingConversation.ts:117-142`, `tgConversationStateRepository.ts:102-110` | **Fix now** |
| 13 | 1 | AC-11 literally requires an active close "without waiting for the client's next message"; implementation only discovers expiry lazily (accepted debt per sad.md §11/ADR-0003/ADR-0005), but spec.md AC-11 itself was never reworded to match. | `conversations/engine.ts:124-138`, `spec.md` AC-11 | **Defer** — soften AC-11 wording in spec §8, owner+due |
| 14 | 2 | Two QG tests assert against their own test doubles (the fake re-implements the exact branch under test), so they'd stay green through a real regression; no test covers an OpenAI-throw path. | `test/.../workoutLoggingQualityGates.test.mjs:34-35,87-95`, `workoutExerciseParser.ts:34` | **Fix now** |
| 15 | 2 | ADR-0005 states ADR-0003's same-type guard "still stands," but `startConversation` still replaces unconditionally — AC-12 rests solely on the `workout_log_session` row, contradicting the ADR text. `BaseRoute.conversationType` doc is also misleading (`MeasurementsRoute` starts a conversation but leaves it `null`). | `tgConversationStateRepository.ts:90-100`, `routes/BaseRoute.ts:4-10`, `routes/MeasurementsRoute.ts:8` | **Fix now** |
| 16 | 2 | Two new Drizzle schema files (`workoutLogSession.ts`, `workoutLogEntry.ts`) are never imported — repository is raw SQL only, so schema can drift from queries with nothing to catch it, unlike every other repository in the repo. | `postgres/schema/workoutLogSession.ts`, `postgres/schema/workoutLogEntry.ts`, `workoutLogging/repository/workoutLogRepository.ts:37-103` | **Fix now** |
| 17 | 2 | Minor convention deviations: hardcoded `kg` unit outside i18n; unused `sessionId` param in `handleExerciseMessage`; `WorkoutLogSession` aggregate root living in a file named after the child entity; service object placed ~90 lines down instead of near the top (AGENTS.md default). | `workoutLoggingConversation.ts:224`, `workoutLoggingService.ts:26-30,91,162-174`, `domain/workoutLogEntry.ts:1` | **Fix now** |
| 18 | 2 | Repo-wide eslint/prettier/tsconfig adoption + ~60 reformatted files landed inside this feature branch, tripling review surface and noising blame/bisect. No strictness weakened; lint is warnings-only. | `eslint.config.mjs`, `.prettierrc.json`, `package.json`, `tsconfig.json` (commits `9501df1`, `25e0d09`) | **Not an issue** — already merged into this branch, not worth unwinding; process feedback for future features noted |

## Gate result

**CHANGES REQUESTED** — 14 fix-now findings, including feature-breaking bugs (permanent session
lockout on `/cancel`, unrelated routes crashing on a malformed lifecycle row, unauthenticated
catalog-id injection via callback data, `/end_workout` unreachable mid-confirmation, and a shared
coach API pagination regression). None may ship as-is.

Deferred (owner: TBD, due: before next release):
- #1 — `/log_workout` re-entry guard (AC-12 unreachable in production)
- #13 — AC-11 wording vs. accepted lazy-expiry behavior

Dismissed:
- #2 — `dict_prompt` row is an operational/seed concern, not a code defect
- #18 — tooling/reformat commits, process-only
